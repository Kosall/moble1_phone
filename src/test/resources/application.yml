spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/dblai
    username: lai
    password: lai01
  jpa:
    properties:
     hibernate:
     dialect: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
     ddl-auto: update
server:
  port: 8084
  
